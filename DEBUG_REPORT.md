# 调试报告：Modbus 编码器字节序 Bug

## 问题现象

发送移动任务（MissionID=3）后，MCU 返回错误：
- **错误码 8**（Failure_OverBound）：任务执行出错，未定义错误
- **错误码 4**（Failure_Aim）：对准修正错误

回零任务（MissionID=2）可以正常完成。

## 根因分析

### 1. 核心 Bug：编码器/圈数字节序错误

**文件**：`BLL/bll_motor.c`

电机驱动（SVD750-RS）的编码器位置是 32 位值，存储在两个 16 位 Modbus 寄存器中：
- 寄存器 0x0066 = 高 16 位
- 寄存器 0x0067 = 低 16 位

Modbus 0x03 响应帧结构：
```
[0]从机地址 [1]0x03 [2]字节数=4 [3]高寄存器高字节 [4]高寄存器低字节 [5]低寄存器高字节 [6]低寄存器低字节
```

**错误代码**（修复前）：
```c
// 高低寄存器的字节被互换了
encoder_number = data[5]<<24 | data[6]<<16 | data[3]<<8 | data[4];
turn_number    = data[5]<<24 | data[6]<<16 | data[3]<<8 | data[4];
```

**正确代码**（修复后）：
```c
// 高寄存器在前，低寄存器在后
encoder_number = data[3]<<24 | data[4]<<16 | data[5]<<8 | data[6];
turn_number    = data[3]<<24 | data[4]<<16 | data[5]<<8 | data[6];
```

### 2. 为什么导致 OverBound

位置计算公式：
```c
position = (encoder_num - zero_encoder_num) * 1000 / 131072
move = distance - position
if (abs(move) > 100000) → 返回 OverBound
```

字节交换是非线性变换，**同一个字节差值乘的权重完全不同**：

| 字节 | 正确权重 | 错误权重 |
|------|---------|---------|
| data[3] (最高字节) | ×2²⁴ | ×2⁸ |
| data[4] | ×2¹⁶ | ×2²⁴ |
| data[5] | ×2⁸ | ×2¹⁶ |
| data[6] (最低字节) | ×1 | ×1 |

**举例**：电机只动了 1 个脉冲（最低字节 +1）：
- 正确差值 = 1
- 错误差值 = 16,777,216（放大了 2²⁴ 倍）

因此 `abs(move) > 100000` 极易触发。

### 3. 为什么回零和移动用相同 bug 不会抵消

假设回零时读到字节 `[A, B, C, D]`，移动时读到 `[A, B, C, D+1]`（只多了 1 个脉冲）：

| | 正确差值 | 错误差值 |
|--|---------|---------|
| 公式 | ΔD × 1 | ΔD × 2²⁴ |
| 结果 | **1** | **16,777,216** |

字节交换把最低字节的差值放到了最高字节的位置，导致差值被放大到天文数字。

### 4. 通信层面验证

通过在 Region 6（Modbus 地址 600）添加调试寄存器，抓取电机返回的原始字节：

```
编码器响应: 03 03 04 7A C0 00 01
圈数响应:   03 03 04 0B 33 00 00
```

- 从机地址 0x03 ✓
- 功能码 0x03 ✓
- 字节数 0x04 ✓
- CRC 正确 ✓

**Modbus 通信完全正常**，问题仅在 MCU 侧的字节解析。

## 修复内容

| 文件 | 修改 |
|------|------|
| `BLL/bll_motor.c` 第 117 行 | `encoder_number` 字节序从 `[5][6][3][4]` 改为 `[3][4][5][6]` |
| `BLL/bll_motor.c` 第 150 行 | `turn_number` 字节序从 `[5][6][3][4]` 改为 `[3][4][5][6]` |

## 额外发现

### data 指针异常

调试中发现 `(u16)data[N]` 存入调试寄存器后，值全部左移了 8 位（如 0x03 变成 0x0300）。这可能与 GD32 的 USART 接收或内存对齐有关，但不影响 C 代码的算术运算（LDRB 指令读取字节值是正确的），暂不影响功能。

### bll_tocase.c 中的硬编码错误码

[bll_tocase.c](BLL/bll_tocase.c) 第 302、395、584 行将 `BLL_Motor_AD_AbsoluteMove` 的返回值统一硬编码为 `*err = 8`：
```c
if (move_rst)
{
    *err = 8;  // 不区分返回值 1（越界）和 2（通信错误）
    goto die;
}
```

建议改为根据 `move_rst` 的实际值区分不同错误类型，便于调试。

## 复现条件

1. 使用 SVD750-RS 电机驱动，通过 Modbus RTU 读取编码器 32 位值
2. 回零后执行移动任务
3. 由于字节序错误，位置计算差值被放大 → 触发 OverBound 或对准修正失败

## 验证方法

1. 编译修复后的固件并烧录
2. **重新执行回零**（旧固件记录的零点值是错误的）
3. 执行移动任务，确认不再报错
4. 用调试脚本 `python test/debug_motor_read.py COMx` 读取 Region 6 验证

## 重要：刷固件后必须重新回零

旧固件用错误字节序记录了 `zero_encoder_num` 和 `zero_turn_num`。新固件用正确字节序读编码器，两者 delta 天差地别，会触发 OverBound。

**每次刷固件后，必须先执行一次回零任务（MissionID=2），之后连续多次移动不需要再回零。**

正确操作流程：
```
刷固件 → 回零(MissionID=2) → 移动A(MissionID=3) → 移动B(MissionID=3) → ... (无需再回零)
```
